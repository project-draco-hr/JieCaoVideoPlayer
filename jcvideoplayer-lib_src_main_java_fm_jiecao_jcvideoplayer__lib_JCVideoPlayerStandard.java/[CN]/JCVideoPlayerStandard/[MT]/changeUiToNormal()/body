{
switch (currentScreen) {
case SCREEN_LAYOUT_LIST:
    setAllControlsVisible(View.VISIBLE,View.INVISIBLE,View.VISIBLE,View.INVISIBLE,View.VISIBLE,coverImageView.getVisibility(),View.INVISIBLE);
  updateStartImage();
break;
case SCREEN_WINDOW_FULLSCREEN:
setAllControlsVisible(View.VISIBLE,View.INVISIBLE,View.VISIBLE,View.INVISIBLE,View.VISIBLE,coverImageView.getVisibility(),View.INVISIBLE);
updateStartImage();
break;
case SCREEN_WINDOW_TINY:
break;
}
}
