{
  if (ifShowTitle) {
    llTitlebg.setVisibility(visable);
  }
 else {
    if (ifFullScreen) {
      llTitlebg.setVisibility(visable);
    }
 else {
      llTitlebg.setVisibility(View.INVISIBLE);
    }
  }
}
